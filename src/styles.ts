// Shared Tailwind class strings for the settings-style pages (Profiles,
// Working Hours, Settings), so the panel/input/button look stays
// consistent without repeating the same class list in every page.

export const sectionClass = "rounded-lg border border-bronze/30 bg-surface-100 p-5";
export const headingClass = "mb-3 text-lg font-semibold text-ink";
export const labelClass = "mb-3 block text-sm text-ink";
export const inputClass =
  "mt-1 block w-full rounded border border-bronze/40 bg-surface-50 p-2 text-ink";
export const buttonClass =
  "rounded bg-bronze px-3 py-1.5 text-sm font-medium text-surface-50 hover:bg-bronze-hover disabled:opacity-40";
